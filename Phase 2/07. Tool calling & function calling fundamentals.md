# 📘 Tool Calling / Function Calling Fundamentals — Complete Guide

**Function calling** (or tool calling) is the ability of large language models (LLMs) to invoke external tools or functions based on the user's request. It's the cornerstone of building **AI agents** that can take actions, fetch real‑time data, or perform computations beyond the model's internal knowledge.

This guide covers everything you need to know to implement function calling in your AI applications.

---

## 1. What Is Function Calling?

Function calling allows an LLM to:
- Understand that certain actions can be performed via external functions.
- Generate a structured request (usually JSON) containing the function name and arguments.
- Hand that request back to your application, which then executes the actual function and returns the result to the LLM.

The LLM **does not execute code** – it only describes which function to call and with what parameters. Your application is responsible for running the function and feeding the result back to the LLM.

### Simple Example

User asks: *"What's the weather in Paris?"*

Without function calling, the LLM might say: *"I don't have real‑time weather data."*

With function calling:
1. You define a function `get_weather(city: string)`.
2. The LLM recognizes the need for weather data and outputs:
   ```json
   { "function": "get_weather", "arguments": { "city": "Paris" } }
   ```
3. Your app calls a weather API, gets the result, and sends it back to the LLM.
4. The LLM then produces a natural‑language answer: *"The weather in Paris is 15°C and sunny."*

---

## 2. Why Is Function Calling Important?

- **Real‑time data** – Access live information (weather, stocks, news).
- **External actions** – Send emails, book appointments, control devices.
- **Computation** – Perform calculations, run code, query databases.
- **Grounding** – Reduce hallucinations by fetching verified facts.
- **Agentic workflows** – Build multi‑step agents that plan and execute actions.

---

## 3. How It Works (Under the Hood)

```
┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐
│  User  │ -> │  LLM   │ -> │  Your  │ -> │  Tool  │
│ Query  │    │        │    │   App  │    │Function│
└────────┘    └────────┘    └────────┘    └────────┘
                   │              │              │
                   └── function ──┘              │
                      call JSON                   │
                                                   │
                   ┌────────┐    ┌────────┐      │
                   │  LLM   │ <- │  App   │ <────┘
                   │        │    │returns│
                   └────────┘    │result │
                        │         └────────┘
                        │
                   natural‑language answer
```

**Steps**:
1. **Define tools** – Provide the LLM with a list of available functions, their descriptions, and parameter schemas (like JSON Schema).
2. **User asks a question** – The LLM decides if any tool should be used.
3. **LLM returns a tool call** – If needed, the response includes a `tool_calls` field with the function name and arguments.
4. **Your app executes the tool** – You validate arguments, run the function (e.g., call an API), and get a result.
5. **Send result back to LLM** – You append the result as a new message (usually with role `tool`) and ask the LLM to continue.
6. **LLM gives final answer** – Using the tool result, it formulates a natural‑language response.

---

## 4. Defining Tools / Functions

You need to provide the LLM with a **tool schema** that describes each function. OpenAI's format (used by many providers) is:

```json
{
  "type": "function",
  "function": {
    "name": "get_weather",
    "description": "Get the current weather in a given city",
    "parameters": {
      "type": "object",
      "properties": {
        "city": {
          "type": "string",
          "description": "The city name, e.g., Paris"
        },
        "unit": {
          "type": "string",
          "enum": ["celsius", "fahrenheit"],
          "description": "Temperature unit"
        }
      },
      "required": ["city"]
    }
  }
}
```

- **name** – The identifier your code will use to call the function.
- **description** – Tells the LLM when to use this function. Be clear and precise.
- **parameters** – A JSON Schema object describing the expected arguments.

### Defining Tools in Code (JavaScript)

For OpenAI's SDK, you pass the tools array when calling the chat completions API.

```javascript
const tools = [
  {
    type: "function",
    function: {
      name: "get_weather",
      description: "Get the current weather in a given city",
      parameters: {
        type: "object",
        properties: {
          city: { type: "string", description: "City name" },
          unit: { type: "string", enum: ["celsius", "fahrenheit"] }
        },
        required: ["city"]
      }
    }
  }
];
```

---

## 5. Implementing Function Calling with OpenAI

### 5.1 Basic Flow

```javascript
import OpenAI from 'openai';

const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });

// Define the tool
const tools = [/* as above */];

// User query
const messages = [
  { role: 'user', content: "What's the weather in Paris?" }
];

// First API call – LLM decides to call a tool
const response = await openai.chat.completions.create({
  model: 'gpt-4o-mini',
  messages,
  tools,
  tool_choice: 'auto' // let the model decide
});

const message = response.choices[0].message;

// Check if the model wants to call a tool
if (message.tool_calls && message.tool_calls.length > 0) {
  const toolCall = message.tool_calls[0];
  const functionName = toolCall.function.name;
  const functionArgs = JSON.parse(toolCall.function.arguments);

  // Execute your function
  let functionResult;
  if (functionName === 'get_weather') {
    functionResult = await getWeather(functionArgs.city, functionArgs.unit);
  }

  // Append the function result as a tool message
  messages.push(message);
  messages.push({
    role: 'tool',
    tool_call_id: toolCall.id,
    content: functionResult
  });

  // Second API call – LLM uses the result to answer
  const secondResponse = await openai.chat.completions.create({
    model: 'gpt-4o-mini',
    messages
  });

  console.log(secondResponse.choices[0].message.content);
}
```

### 5.2 Example Tool Implementation

```javascript
async function getWeather(city, unit = 'celsius') {
  // In real life, call a weather API (e.g., OpenWeatherMap)
  // Simulate for demonstration
  const mockData = {
    Paris: { celsius: 15, fahrenheit: 59 },
    London: { celsius: 12, fahrenheit: 54 },
    // ...
  };
  const temp = mockData[city]?.[unit] || 'unknown';
  return `The weather in ${city} is ${temp}°${unit === 'celsius' ? 'C' : 'F'}.`;
}
```

### 5.3 Parallel Function Calls

OpenAI supports calling multiple tools in one response. The `tool_calls` array may contain several entries. You should execute them all (possibly in parallel) and return results for each, associating them via `tool_call_id`.

```javascript
if (message.tool_calls) {
  // Execute all tool calls in parallel
  const results = await Promise.all(message.tool_calls.map(async (toolCall) => {
    const { name, arguments: args } = toolCall.function;
    const parsedArgs = JSON.parse(args);
    // dispatch based on name
    const result = await yourFunctionDispatcher(name, parsedArgs);
    return { tool_call_id: toolCall.id, result };
  }));

  // Append each result as a tool message
  results.forEach(({ tool_call_id, result }) => {
    messages.push({ role: 'tool', tool_call_id, content: result });
  });

  // Then call again
}
```

---

## 6. Working with LangChain.js

LangChain simplifies tool calling with its `Tool` interface and agent frameworks.

### 6.1 Defining a Tool

```javascript
import { DynamicStructuredTool } from '@langchain/core/tools';
import { z } from 'zod';

const weatherTool = new DynamicStructuredTool({
  name: 'get_weather',
  description: 'Get current weather for a city',
  schema: z.object({
    city: z.string().describe('City name'),
    unit: z.enum(['celsius', 'fahrenheit']).optional().default('celsius')
  }),
  func: async ({ city, unit }) => {
    // your implementation
    return `Weather in ${city} is ...`;
  }
});
```

### 6.2 Using with an Agent

```javascript
import { ChatOpenAI } from '@langchain/openai';
import { createReactAgent } from '@langchain/langgraph/prebuilt';

const llm = new ChatOpenAI({ model: 'gpt-4o-mini' });
const agent = await createReactAgent({ llm, tools: [weatherTool] });

const result = await agent.invoke({
  messages: [{ role: 'user', content: "What's the weather in Paris?" }]
});
console.log(result.messages[result.messages.length - 1].content);
```

LangChain handles the entire loop: model calls tool, executes it, feeds back, and returns final answer.

---

## 7. Best Practices

### 7.1 Write Clear Descriptions
- Explain **when** the tool should be used.
- Include edge cases: e.g., "Use this for real‑time weather queries only."

### 7.2 Use Enums and Constraints
- Define allowed values with `enum` to prevent invalid arguments.
- Mark required fields clearly.

### 7.3 Validate Arguments on Your Side
- Even with schemas, validate arguments before executing (defensive programming).

### 7.4 Handle Errors Gracefully
- If the tool fails, return an error message (as string) so the LLM can explain the issue.

```javascript
try {
  result = await actualTool(args);
} catch (e) {
  result = `Error: ${e.message}`;
}
```

### 7.5 Keep Tool Outputs Concise
- Return only what the LLM needs. Long outputs waste tokens.

### 7.6 Use Tool Choice Carefully
- `tool_choice: 'auto'` – model decides.
- `tool_choice: 'none'` – force no tool.
- `tool_choice: { type: 'function', function: { name: '...' } }` – force a specific tool.

### 7.7 Parallel Execution
- If multiple tools are called, run them concurrently to reduce latency.

---

## 8. Example: Multi‑Tool Assistant

Let's build a simple assistant with two tools: `get_weather` and `calculator`.

```javascript
import OpenAI from 'openai';

const openai = new OpenAI();

const tools = [
  {
    type: "function",
    function: {
      name: "get_weather",
      description: "Get current weather",
      parameters: {
        type: "object",
        properties: { city: { type: "string" } },
        required: ["city"]
      }
    }
  },
  {
    type: "function",
    function: {
      name: "calculator",
      description: "Perform basic arithmetic",
      parameters: {
        type: "object",
        properties: {
          a: { type: "number" },
          b: { type: "number" },
          op: { type: "string", enum: ["+", "-", "*", "/"] }
        },
        required: ["a", "b", "op"]
      }
    }
  }
];

async function run(query) {
  const messages = [{ role: "user", content: query }];
  
  let response = await openai.chat.completions.create({
    model: "gpt-4o-mini",
    messages,
    tools
  });
  
  let message = response.choices[0].message;
  messages.push(message);
  
  while (message.tool_calls && message.tool_calls.length > 0) {
    // Execute all tool calls
    for (const toolCall of message.tool_calls) {
      const { name, arguments: args } = toolCall.function;
      const parsed = JSON.parse(args);
      let result;
      
      if (name === "get_weather") {
        result = await getWeather(parsed.city);
      } else if (name === "calculator") {
        result = await calculator(parsed.a, parsed.b, parsed.op);
      }
      
      messages.push({
        role: "tool",
        tool_call_id: toolCall.id,
        content: result
      });
    }
    
    // Get next response from LLM
    response = await openai.chat.completions.create({
      model: "gpt-4o-mini",
      messages
    });
    message = response.choices[0].message;
    messages.push(message);
  }
  
  return message.content;
}

// Tool implementations
async function getWeather(city) { /* ... */ }
async function calculator(a, b, op) {
  switch(op) {
    case '+': return (a + b).toString();
    case '-': return (a - b).toString();
    case '*': return (a * b).toString();
    case '/': return (a / b).toString();
    default: return "Invalid operator";
  }
}
```

---

## 9. Function Calling with Other Providers

- **Anthropic Claude** – Similar tool use via `tools` parameter (beta).
- **Google Gemini** – Supports function calling via `tools` and `tool_config`.
- **Mistral / Cohere** – Also offer tool‑calling capabilities.

The concepts are the same; only the API shape differs slightly.

---

## 10. Limitations and Considerations

- **Token usage** – Tool definitions and results consume tokens. Keep schemas concise.
- **Latency** – Multiple round trips increase response time. Use parallel execution.
- **Model may misuse tools** – Clear descriptions and examples in the prompt help.
- **Security** – Never expose dangerous functions (e.g., delete database). Validate all inputs.

---

## 11. Next Steps

Now that you understand function calling:

- Build an **agent** that can use multiple tools.
- Integrate with **LangChain** or **LlamaIndex** to manage tool‑augmented agents.
- Explore **multi‑turn conversations** where the model calls tools repeatedly.
- Learn about **ReAct** (Reason + Act) pattern for more complex agentic behavior.

---

## 📝 Hands-On Exercise

1. Create a tool that fetches current time for a given timezone (use an API like WorldTime).
2. Build a simple assistant that can answer both weather and time queries.
3. Implement a calculator tool and ask multi‑step questions like: *"What's the weather in New York and then add 5 to the temperature?"*
4. Experiment with forcing a specific tool (`tool_choice`) vs. letting the model decide.

---

**Function calling unlocks the true potential of LLMs – transforming them from conversational chatbots into action‑oriented agents.** Master this, and you're well on your way to building sophisticated AI systems.
